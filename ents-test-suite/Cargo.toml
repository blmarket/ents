[package]
name = "ents-test-suite"
version = "0.5.0"
authors = ["blmarket@gmail.com"]
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Ents database implementation test suite"
repository = "https://github.com/blmarket/ents"

[dependencies]
ents.workspace = true
ents-admin.workspace = true
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
typetag = "0.2"
